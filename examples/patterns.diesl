# Pattern matching examples for extraction and replacement.
# Using both the Nim-syntax and the macro-based natural syntax.

db.students.email = db.students.data.extractOne(r";{email};")
#db.students[firstName, secondName] = db.students.name.extractAll(r"([a-z]+) ([a-z]+)")

# TODO: natural syntax example
